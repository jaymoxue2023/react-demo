// use rest operator to pass in props so that we don't have to pass in all props individually
export default function Section({ title, children, ...props }) {
  return (
    <section {...props}>
      <h2>{title}</h2>
      {children}
    </section>
  );
}
